<%= simple_form_for(mood) do |f| %>
  <% if mood.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mood.errors.count, "error") %> prohibited this mood from being saved:</h2>

      <ul>
      <% mood.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :feeling, :as => :select, :collection => Mood.feelings.keys.to_a %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
